Para executar a aplicação:

 - Clonar o repositório
 - Composer install
 - Configurar conexão ao banco de dados no arquivo .env
 - executar os seguintes comandos:
    - php artisan migrate
    - php artisan serve